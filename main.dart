import 'package:flutter/material.dart';
import 'task1/image.dart';
import 'task1/video.dart';

main() => runApp(MyApp());

class MyApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return myvideo();
  }
}
